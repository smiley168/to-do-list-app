mutation {
  todo1: createTodoItem(data: { text: "Buy Toilet Paper", completed: false }) {
    id
  }
  todo2: createTodoItem(data: { text: "Get Dog Food", completed: false }) {
    id
  }
  todo3: createTodoItem(data: { text: "Bid on Hand Sanitizers on EBay", completed: false }) {
    id
  }
}
